# member 数据库变更日志

## [B20260319] - 2026-03-23

### Baseline

- `B20260319__full_schema.sql`：唯一前向基线脚本，已整合历史全部前向变更，直接用于初始化最新会员认证结构

### Rollback

- `R20260319__full_schema.sql`：唯一回滚脚本，用于回滚当前完整会员认证结构

## 当前目录约定

- `baseline`：只保留一个可直接初始化最新结构的前向脚本
- `rollback`：只保留一个可直接回滚当前结构的脚本
- `migration`：不再保留前向迁移脚本
